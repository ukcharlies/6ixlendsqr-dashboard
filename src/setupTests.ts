import { enableFetchMocks } from "jest-fetch-mock";
// Removed invalid import of Future from "@remix-run/router"

// Enable fetch mocks
enableFetchMocks();

// Removed configuration of React Router future flags as Future is not exported
